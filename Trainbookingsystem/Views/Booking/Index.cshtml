@* @model IEnumerable<Trainbookingsystem.Models.BookingTicket>

@{
    ViewData["Title"] = "Booking List";
}

<h1 class="text-center">Booking List</h1>

<div class="container">
    @if (!Model.Any())
    {
        <p class="text-center">No bookings available.</p>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>Booking ID</th>
                        <th>Train ID</th>
                        <th>Passenger Name</th>
                        <th>Passenger Email</th>
                        <th>Number of Tickets</th>
                        <th>Total Price</th>
                        <th>Booking Time</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var booking in Model)
                    {
                        <tr>
                            <td>@booking.BookingId</td>
                            <td>@booking.TrainId</td>
                            <td>@booking.PassengerName</td>
                            <td>@booking.PassengerEmail</td>
                            <td>@booking.NumberOfTickets</td>
                            <td>@String.Format("{0:C}", booking.TotalPrice)</td> <!-- Format total price as currency -->
                            <td>@booking.BookingTime.ToString("yyyy-MM-dd HH:mm:ss")</td> <!-- Format booking time -->
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
 *@